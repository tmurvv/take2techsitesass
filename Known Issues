get ion icons working in this project - then add linkedin icon on bottom. -- refer to Natours project for JSON with icons
    JSON ENTRY FOR ICON FILE --- "concat:css": "concat -o css/style.concat.css css/icon-font.css css/style.compiled.css",
    Also add concat to prefix command to get sequence right
    Also add concat:css to build command

glossary sticky headings not lined up on portrait

Build breaks image retrieval

back of card visit site btton not underline on hover

phone about my image obscures text, hamburger menu needs more right margin.

when cards flipping, should not be back button

MAC Laptop-- shadows around header elements
iOS projects subtext not centered, but right aligned